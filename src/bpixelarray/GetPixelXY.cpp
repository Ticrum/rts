#include "Bpixelarray.hh"
/*
unsigned int &ef::Bpixelarray::GetSetPixel(unsigned int x,
                               unsigned int y)
{
if(px == NULL)
throw(-1);
return pixels[x + px->clipable.buffer.width * y];
}
*/
